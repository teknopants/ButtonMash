namespace ButtonMash
{
    public enum ActionState
    {
        Pressed,
        Held,
        Released,
        None
    }
    
    public enum Player
    {
        One,
        Two,
        Three
    }
}